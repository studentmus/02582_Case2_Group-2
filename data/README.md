# Data folder

Place the dataset locally here.

Expected usage:
- `data/raw/` for original files
- `data/interim/` for temporary cleaned/merged files
- `data/processed/` for model-ready matrices

Do not commit data files to git.
